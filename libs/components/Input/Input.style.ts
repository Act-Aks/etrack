import { StyleSheet } from 'react-native-unistyles'

export const inputStyles = StyleSheet.create({
    input: {
        height: 40,
        borderColor: 'gray',
        borderWidth: 1,
        padding: 10,
        marginVertical: 10,
    },
})
